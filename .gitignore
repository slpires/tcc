# ==============================================================================
# SLPIRES.COM – TCC UFF
# .gitignore institucional – versão padronizada (2025)
# ==============================================================================

# [AMBIENTE] Configurações sensíveis e arquivos de ambiente
config/config.php
.env
*.env
!.env.example

# [LOGS] Bloqueio total de logs e arquivos temporários gerados pelo sistema
logs/
logs/.htaccess           # Só versionar logs/.htaccess se documentado em histórico
*.log
*.tmp
*.cache
*.bak
*.old

# [BANCO DE DADOS] Arquivos de backup, exportações e históricos SQL
db/*.bak
db/*.sql.bak
db/*.backup.sql

# [TESTES E DOCUMENTAÇÃO] Arquivos/pastas de evidências temporárias e prints
tests/tmp/
docs/tmp/
docs/prints/

# [SISTEMA OPERACIONAL]
.DS_Store
Thumbs.db

# [IDE/EDITORES]
.vscode/
*.code-workspace

# [HOSPEDAGEM/FTP] Infra do servidor (fora do escopo do app)
.well-known/
.ftpquota

# [.HTACCESS]
# Regra geral: ignorar .htaccess...
.htaccess
# ...mas EXCETUAR o .htaccess DA APLICAÇÃO (roteamento/segurança DEV=PRD)
!public/.htaccess
# manter exceção já prevista
!logs/.htaccess
# (se o repositório for versionado a partir de /tcc/, a linha acima cobre;
# se houver outra raiz, acrescentar a exceção com caminho relativo adequado.)

# [XAMPP] Ambiente local
xampp/
xamppfiles/

# [PADRÃO TCC] PHP de desenvolvimento/teste
*index_*.php
config/teste.php

# ==============================================================================
# Fim do .gitignore institucional padronizado – SLPIRES.COM | TCC UFF
# Regras detalhadas e rastreabilidade obrigatória em histórico/documentação
# ==============================================================================
